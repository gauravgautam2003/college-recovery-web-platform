export default function CollegeFilters() {
    return (
        <section className="space-y-2 sm:space-y-3 rounded-xl border border-slate-200 bg-white p-3 sm:p-4 shadow-sm">
            <h3 className="text-base sm:text-lg font-semibold">Filters</h3>
            <p className="text-xs sm:text-sm text-slate-600">Add filters for location, ranking, and type.</p>
        </section>
    );
}
